import { VARIANTS } from './variants';

export const FADE_UP = VARIANTS.fadeUp(false);
export const FADE_IN = VARIANTS.fadeIn(false);
export const SCALE_UP = VARIANTS.scaleUp(false);
export const STAGGER_CONTAINER = VARIANTS.staggerContainer;
